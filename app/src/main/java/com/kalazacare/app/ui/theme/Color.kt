package com.kalazacare.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Kalaza Care Brand Colors ─────────────────────────────────────────────────
val KalazaRed        = Color(0xFF9B000A)   // Primary brand red – buttons, FABs, accents
val KalazaLightRed   = Color(0xFFC80714)   // Hover / pressed red
val KalazaDarkMaroon = Color(0xFF4C0404)   // Status bar, top bar background
val KalazaNavy       = Color(0xFF0C0367)   // Data/chart accent

// ── Surface Tones ─────────────────────────────────────────────────────────────
val White            = Color(0xFFFFFFFF)
val SurfaceVariant   = Color(0xFFF8F8F8)   // Alternating table rows, card backgrounds
val Outline          = Color(0xFFDFE0E0)   // Dividers, borders

// ── Text ──────────────────────────────────────────────────────────────────────
val OnSurface        = Color(0xFF1A1A1A)   // Primary text
val OnSurfaceVariant = Color(0xFF69727D)   // Secondary / subtitle text

// ── Semantic Status Colors ────────────────────────────────────────────────────
val StatusSuccess    = Color(0xFF00D084)   // Approved / administered
val StatusWarning    = Color(0xFFFCB900)   // Pending
val StatusError      = Color(0xFFCF2E2E)   // Rejected / overdue

// ── On-Color Variants ─────────────────────────────────────────────────────────
val OnRed            = Color(0xFFFFFFFF)
val OnNavy           = Color(0xFFFFFFFF)
